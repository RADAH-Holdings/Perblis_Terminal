// Placeholder. Regenerate with `npm run gen:api` once the Django backend
// is reachable at NEXT_PUBLIC_API_BASE_URL (default http://localhost:8000).
// The schema endpoint is /api/schema/?format=json — see scripts/generate-api-types.sh.
export type placeholder = unknown;
