{
  "name": "terminal-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "tokens:build": "pnpm --filter @terminal/tokens build",
    "portal:dev": "pnpm --filter @terminal/portal dev",
    "portal:build": "pnpm --filter @terminal/portal build"
  }
}
