# Glyphs

Class glyphs, the brand T-crane, and stamp SVGs (design-system ch.01/06).
Placeholder for Wave 0 — the full glyph set is added with the component specs
it serves ("no orphan UI", ch.10 §5). `build.ts` ensures this directory exists
so consumers can resolve the export path.
