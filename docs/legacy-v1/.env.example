SECRET_KEY=django-insecure-change-this-in-production-use-50-chars-minimum
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOW_ALL_ORIGINS=True
# Production (DJANGO_SETTINGS_MODULE=config.settings.production): list every browser
# origin that calls the API (comma-separated, include scheme). OWNER_WEB_URL is
# appended automatically if set (e.g. your Railway Next.js URL).
# CORS_ALLOWED_ORIGINS=https://your-owner-web.up.railway.app
# OWNER_WEB_URL=https://your-owner-web.up.railway.app

DB_NAME=terminal_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# Local dev (config.settings.development) uses R2_BUCKET_NAME / R2_ENDPOINT_URL / R2_CUSTOM_DOMAIN.
R2_BUCKET_NAME=terminal-uploads
R2_ENDPOINT_URL=
R2_CUSTOM_DOMAIN=
# Production (config.settings.production) requires these exact names (see production.py):
# R2_BUCKET=your-bucket
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# R2_PUBLIC_URL=https://pub-<id>.r2.dev   (or your custom public domain; scheme optional)

ABLY_API_KEY=
MAPBOX_ACCESS_TOKEN=
SENTRY_DSN=
